# Copy this to `.env.development.local` (gitignored) and tweak as needed.
#
# Local dev: point at your Docker backend on localhost.
# Production: this file is NOT used — set the var in your Vercel project settings.

REACT_APP_API_BASE_URL=http://localhost:8000/birds/api
