REACT_APP_API_BASE_URL=https://api.nzbirddatabase.com/birds/api

# Skip ESLint during production build. CRA promotes warnings to errors when
# CI=true (which Vercel always sets), and we have a few harmless warnings
# (unused imports, useEffect deps). Re-enable when we clean those up.
DISABLE_ESLINT_PLUGIN=true
